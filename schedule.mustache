<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">

    <title>{{docTitle}}</title>

    <style>
        h1 { font-size: 133%; }
        h2 { font-size: 125%; }
        .dayHead { margin-top: 1em; }
    </style>
  </head>
  <body>
    <div class="container-fluid">
        <h1>MN Fringe 2022 Global Schedule</h1>
        <p class="small">Updated at {{buildTime}}</p>
        <div class="row dayNav">
                 <div class="col-1">Day:</div>
                {{#dayNav}}
                    <div class="col-1">
                        <a href="#day{{dayNum}}">{{dateStr}}</a>
                    </div>
                {{/dayNav}}
        </div>
        {{#days}}

                <h2 class="dayHead" id="day{{dayNum}}">{{dayLabel}}</h2>

                {{#events}}
                    <div class="row event border-bottom">
                        <div class="col-12 col-lg-5"><a target="_blank" href="{{showUrl}}">{{showTitle}}</a> <span class="badge">{{serviceTags}}</span> </div>
                        <div class="col-2  col-lg-1">{{date}}</div>
                        <div class="col-3  col-lg-2">{{time}}</div>
                        <div class="col-7  col-lg-4">{{venue}}</div>
                    </div>
                {{/events}}
        {{/days}}
    </div>
  </body>
</html>